apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simbad2k.fullname" . }}-test-connection"
  labels:
{{ include "simbad2k.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "simbad2k.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
